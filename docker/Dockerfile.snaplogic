FROM alpine:3.18
RUN apk add --no-cache curl
EXPOSE 8080
CMD ["sh", "-c", "echo 'SnapLogic Groundplex Local Node Simulator Active' && while true; do echo -e 'HTTP/1.1 200 OK\r\nContent-Type: text/plain\r\nConnection: close\r\n\r\nSnapLogic Groundplex Node OK' | nc -lp 8080; done"]
